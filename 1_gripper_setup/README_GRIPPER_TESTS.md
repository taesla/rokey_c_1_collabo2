# OnRobot RG2 그리퍼 테스트 파일 정리

## 📁 파일 목록

### 1. test_gripper_connection.py
**목적**: 그리퍼 연결 확인  
**사용 시기**: 그리퍼가 처음 연결되었을 때, 네트워크 문제 해결  
**테스트 항목**:
- 네트워크 연결 (192.168.1.1)
- 그리퍼 상태 읽기
- 현재 너비 확인

**실행**:
```bash
python3 test_gripper_connection.py
```

---

### 2. test_gripper_basic.py
**목적**: 기본 그리퍼 동작 테스트  
**사용 시기**: 그리퍼 기본 기능 확인  
**테스트 항목**:
- 그리퍼 열기 (open_gripper)
- 그리퍼 닫기 (close_gripper)
- 특정 위치로 이동 (move_gripper)
- 동작 완료 대기 (status 체크)

**실행**:
```bash
python3 test_gripper_basic.py
```

**주요 함수**:
- `gripper.open_gripper()` - 최대로 열기
- `gripper.close_gripper()` - 완전히 닫기
- `gripper.move_gripper(width_val, force_val)` - 정밀 제어

---

### 3. test_gripper_advanced.py
**목적**: 고급 기능 및 정밀 제어 테스트  
**사용 시기**: 세밀한 그리핑 작업, 힘 제어 필요 시  
**테스트 항목**:
- Fingertip Offset 확인
- Width 측정 (오프셋 포함/미포함)
- 다양한 힘으로 그리핑 (50N, 200N)
- 정밀 위치 제어 (80mm, 60mm, 40mm, 20mm)
- 상태 플래그 상세 분석

**실행**:
```bash
python3 test_gripper_advanced.py
```

**주요 함수**:
- `gripper.get_fingertip_offset()` - 핑거팁 오프셋 값
- `gripper.get_width()` - 오프셋 미포함 너비
- `gripper.get_width_with_offset()` - 오프셋 포함 너비
- `gripper.get_status()` - 7개 상태 플래그
- `gripper.close_gripper(force_val)` - 힘 조절 닫기

---

## 🔧 사용 순서 (권장)

1. **연결 확인**: `test_gripper_connection.py`
   - 네트워크가 제대로 설정되었는지 확인
   - 그리퍼 응답 확인

2. **기본 동작**: `test_gripper_basic.py`
   - 열기/닫기 기본 동작 확인
   - 간단한 위치 제어

3. **고급 제어**: `test_gripper_advanced.py`
   - 정밀한 위치 제어
   - 힘 제어 테스트
   - 상태 모니터링

---

## 📊 비교표

| 파일 | 난이도 | 테스트 시간 | 주요 용도 |
|------|--------|------------|----------|
| connection | ⭐ | 5초 | 연결 확인 |
| basic | ⭐⭐ | 30초 | 기본 동작 |
| advanced | ⭐⭐⭐ | 1분 | 고급 기능 |

---

## 🚨 문제 해결

### 연결 실패 시
1. `test_gripper_connection.py` 실행
2. 에러 메시지 확인
3. 네트워크 설정 체크 (192.168.1.100 ↔ 192.168.1.1)

### 동작 이상 시
1. `gripper.get_status()` 확인
2. Safety 플래그 체크
3. 그리퍼 전원 재시작

---

## 💡 실전 예제

### 물체 집기
```python
from onrobot import RG
gripper = RG("rg2", "192.168.1.1", "502")

# 열기
gripper.open_gripper()
while gripper.get_status()[0]:
    time.sleep(0.3)

# 물체 위치로 이동 (로봇팔 코드)

# 부드럽게 닫기 (100N)
gripper.close_gripper(force_val=1000)
while gripper.get_status()[0]:
    time.sleep(0.3)

# 물체 감지 확인
if gripper.get_status()[1]:
    print("물체를 잡았습니다!")
```
