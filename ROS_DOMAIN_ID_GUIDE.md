# ROS_DOMAIN_ID 설정 가이드

## 🎯 목적
여러 조가 같은 WiFi 네트워크를 사용할 때 ROS2 통신을 조별로 격리

## 👥 우리 조 구성
- 노트북 4대
- 로봇팔 1대
- 할당된 ID: **60~65번**

---

## ⚙️ 설정 방법

### 방법 1: 자동 스크립트 (권장)

**각 노트북에서 실행:**
```bash
cd ~
./setup_ros_domain_id.sh
```

**로봇팔 티치펜던트 설정:**
- 티치펜던트 → 설정 → ROS2 → DOMAIN_ID → **60** 입력

---

### 방법 2: 수동 설정

**각 노트북에서:**
```bash
# bashrc에 추가
echo 'export ROS_DOMAIN_ID=60' >> ~/.bashrc

# 즉시 적용
source ~/.bashrc

# 확인
echo $ROS_DOMAIN_ID
```

---

## 🔄 설정 확인

### 1. 환경 변수 확인
```bash
echo $ROS_DOMAIN_ID
# 출력: 60
```

### 2. ROS2 통신 테스트

**노트북 1에서:**
```bash
ros2 topic pub /test std_msgs/msg/String "data: 'Hello from Laptop 1'" -r 1
```

**노트북 2, 3, 4에서:**
```bash
ros2 topic list
# /test 토픽이 보이면 성공!

ros2 topic echo /test
# "Hello from Laptop 1" 메시지가 보이면 성공!
```

### 3. 로봇팔 연결 확인
```bash
ros2 topic list | grep dsr
# /dsr01/... 토픽들이 보이면 로봇팔과 통신 중
```

---

## 📋 조별 ID 할당 예시

| 조 | ROS_DOMAIN_ID | 사용 범위 |
|----|---------------|----------|
| 1조 | 60 | 모든 장비 60번 사용 |
| 2조 | 61 | 모든 장비 61번 사용 |
| 3조 | 62 | 모든 장비 62번 사용 |
| 4조 | 63 | 모든 장비 63번 사용 |

**권장**: 조 내 모든 장비(노트북4대+로봇)를 **같은 ID**로 설정

---

## 🚨 문제 해결

### Q: 다른 조의 토픽이 보여요
**A**: ROS_DOMAIN_ID가 제대로 설정되지 않음
```bash
# 확인
echo $ROS_DOMAIN_ID

# 재설정
export ROS_DOMAIN_ID=60
source ~/.bashrc
```

### Q: 같은 조 노트북끼리 통신 안 돼요
**A**: 모든 노트북의 ID가 같은지 확인
```bash
# 각 노트북에서
echo $ROS_DOMAIN_ID
# 모두 60이어야 함
```

### Q: 로봇팔이 안 보여요
**A**: 
1. 로봇팔 티치펜던트에서 ROS_DOMAIN_ID 확인
2. 로봇팔과 노트북이 같은 ID인지 확인
3. 로봇 드라이버가 켜져있는지 확인

---

## 💡 고급 옵션 (선택사항)

### 조 내에서 역할 분리가 필요한 경우

```bash
# 노트북 1 (메인 제어)
export ROS_DOMAIN_ID=60

# 노트북 2 (시각화)
export ROS_DOMAIN_ID=60

# 노트북 3 (데이터 수집)
export ROS_DOMAIN_ID=60

# 노트북 4 (백업/테스트)
export ROS_DOMAIN_ID=61  # 격리해서 테스트

# 로봇팔
ROS_DOMAIN_ID=60
```

**주의**: 다른 ID를 쓰면 서로 통신 안 됨!

---

## ✅ 체크리스트

설정 완료 후 확인:

- [ ] 노트북 1: `echo $ROS_DOMAIN_ID` → 60
- [ ] 노트북 2: `echo $ROS_DOMAIN_ID` → 60
- [ ] 노트북 3: `echo $ROS_DOMAIN_ID` → 60
- [ ] 노트북 4: `echo $ROS_DOMAIN_ID` → 60
- [ ] 로봇팔: 티치펜던트에서 60 확인
- [ ] 노트북간 토픽 통신 테스트 성공
- [ ] 다른 조 토픽이 안 보임 확인
